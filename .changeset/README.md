# Changesets

Hello and welcome! This folder has been automatically generated by `@changesets/cli`, a build tool that works
with multi-package repos, or single-package repos to help you version and publish your code. You can
find the full documentation for it [in our repository](https://github.com/changesets/changesets).

## Workflow

**1. Make a change** in any package.

**2. Record the intent:**

```bash
pnpm changeset
```

Pick which packages changed, the semver bump (patch/minor/major), and write a one-line summary.
Commit the generated `.changeset/*.md` alongside your code.

**3. When ready to release:**

```bash
pnpm changeset version   # bumps package versions + updates cross-package deps + writes changelogs
pnpm build               # tsup builds everything
pnpm changeset publish   # pushes to npm in topological order (workspace: & catalog: → real versions)
```

## Full docs

- [Common Questions](https://github.com/changesets/changesets/blob/main/docs/common-questions.md)
- [Automating with GitHub Actions](https://github.com/changesets/changesets/blob/main/docs/automating-changesets.md)
