/**
 * @file index.ts
 * @module @stackra/ssr/react
 * @description React subpath — placeholder for hooks, `<Meta>`, `<Link>`,
 *   `<StackraRouter>` components. Content is populated by a follow-up
 *   spec (React Router v7 library-mode wiring).
 */

export {};
