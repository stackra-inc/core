/**
 * @file index.ts
 * @module @stackra/ssr
 * @description Root barrel — re-exports the middleware surface for the
 *   `@stackra/ssr` root import. Additional server/react/vite subpaths
 *   are exposed via their own subpath entries (`./server`, `./react`, ...).
 */

export * from '../middleware';
