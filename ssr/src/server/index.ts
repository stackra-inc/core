/**
 * @file index.ts
 * @module @stackra/ssr/server
 * @description Server-side subpath — placeholder for the SSR renderer,
 *   streaming, and HTTP adapters. Content is populated by a follow-up
 *   spec (SSR renderer). Left as an intentional stub so the subpath
 *   ships from day 1 and consumers can pin the import path.
 */

export {};
