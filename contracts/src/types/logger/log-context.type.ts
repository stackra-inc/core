/**
 * @file log-context.type.ts
 * @module @stackra/contracts/types/logger
 * @description Structured metadata attached to log entries.
 */

/** Structured metadata attached to log entries. */
export type LogContext = Record<string, unknown>;
