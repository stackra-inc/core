/**
 * @file sdui-mode.type.ts
 * @module @stackra/contracts/types/sdui
 * @description Re-export `SduiMode` from the enum file for callers that
 *   prefer importing from `types/sdui` rather than `enums/sdui`.
 */

export type { SduiMode } from '../../enums/sdui/sdui-mode.enum';
