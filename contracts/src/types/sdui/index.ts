/**
 * @file index.ts
 * @module @stackra/contracts/types/sdui
 * @description Barrel re-exports for every SDUI type alias.
 */

export type {
  ITranslatableKey,
  ITranslatableValues,
  TranslatableText,
} from './translatable-text.type';

export type { SduiMode } from './sdui-mode.type';
